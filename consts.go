package gofire

const (
	// The only type of content Geode currently accepts.
	ContentType = "application/json"
	// The basepath of the API.
	BaseURIPath = "/gemfire-api"
	// Current API Version.
	CurrentAPIVersion = "/v1"
	UserAgent = "gofire/1.0"
)
